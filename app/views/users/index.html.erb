<% provide(:title, 'All users') %>
<h3>All users</h3>

<ul class="all_users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
<%= paginate @users %><br><%= page_entries_info @users %>
</ul>