<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items, :params => {:controller => 'static_pages', :action => 'home'} %>
  <br>
  <%= page_entries_info @feed_items %>
<% end %>